doctype html
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(http-equiv="X-UA-Compatible", content="IE=edge")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body 
        h2 Reestablecer contraseña
        p Hola, has solicitado reestablecer tu contraseña, haz "click" en el siguiente enlace para reestablecerla, este enlace es temporal, asi que si se vene vuelve a solicitarlo.
        a(href=`${resetUrl}`) Ir a Reestablecer contraseña
        p Si no puedes acceder a este enlace, visita #{resetUrl}
        p Si no solicitaste este e-mail, puedes ignorarlo 